// export const API_URL =
//   `${window.location.protocol}//` +
//   `${window.location.hostname}:3000/api`;

// The browser will automatically attach the correct protocol, IP, and port of whoever is visiting
export const API_URL = '/api'; 
